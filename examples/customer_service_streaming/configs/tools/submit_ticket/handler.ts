export function submitTicket(description: string): { response: string } {
  return { response: `ticket created for ${description}` };
}

export function submitTicketAssistants(description: string): { response: string } {
  return { response: `ticket created for ${description}` };
}
